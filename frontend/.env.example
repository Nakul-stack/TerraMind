# Frontend runtime configuration
# Public, non-secret values only. Do NOT place API keys or credentials here.

# Backend origin used by the browser app
VITE_API_BASE_URL=http://localhost:8000

# Request timeout for growth-stage monitor calls
VITE_MONITOR_TIMEOUT_MS=30000
